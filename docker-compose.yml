version: '3.8'

services:
  drone-opencv:
    build: .
    container_name: drone-opencv-dev
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true
    command: /bin/bash
    
    # NOTE: This Docker setup is for development/dependency management only
    # For actual validation testing with the capture card, run on macOS host:
    #   python validation_suite.py --camera 1
